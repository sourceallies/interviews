def package_url(package_name: str) -> str:
    return f"https://registry.npmjs.org/{package_name}"

async def list_transitive_dependencies(package_name: str) -> list:
    # Placeholder for async operation to fetch transitive dependencies
    return []
